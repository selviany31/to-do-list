import React from 'react'
import '../Styles/Card.css'

const Card = ({ styleName }) => {
    return (
        <div className={styleName}>
            lalalal
        </div>
    )
}

export default Card